# example of a drive with project files

[drives.data]
builder = "directory"
directory = "./data" # required
extraSize = "100Mb" # optional. size is given by directory content size plus this amount
mount = "/var/lib/app" # optional, default is /mnt/{name}
