services:
    database:
        image: postgres:16-alpine
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U postgres || exit 1"]
            interval: 200ms
            timeout: 500ms
            retries: 20
        environment:
            - POSTGRES_PASSWORD=password
