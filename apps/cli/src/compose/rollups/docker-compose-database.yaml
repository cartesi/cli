services:
    database:
        image: postgres:16
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U postgres || exit 1"]
            start_period: 5s
            start_interval: 200ms
            timeout: 500ms
            retries: 20
        environment:
            - POSTGRES_PASSWORD=password
        volumes_from:
            - anvil
