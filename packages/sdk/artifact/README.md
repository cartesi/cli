### Artifact

Add the rollups node binary inside this folder. For version sake, below is the link to the same binary used to generate a build of CLI where the SDK docker image contains the expected rollups-node.

Rollups node build: https://github.com/cartesi/rollups-node/actions/runs/16057987531

Remember to rename the binary to `cartesi-rollups-node.deb`, that is what the the sdk dockerfile will be looking for.
